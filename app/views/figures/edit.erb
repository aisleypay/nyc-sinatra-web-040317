<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit Figure</title>
  </head>
  <body>
    <form action="/figures/<%=@figure.id%>" method=POST>
      <h1>Edit <%= @figure.name%></h1>
      <p>Figure Name: <input type="text" id ="figure_name" name="figure[name]" value="<%=@figure.name%>"</input></p>

      <br></br>

      <label>Figure Titles:</label>

      <% Title.all.each do |title| %>
        <%if @figure.titles.include?(title)%>
          <input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="title_<%=title.id%>" checked><%= title.name %></input>
        <%else %>
          <input type="checkbox" name="figure[title_ids][]" value="<%=title.id%>" id="title_<%=title.id%>"><%= title.name %></input>
        <%end%>
      <%end%>

      <p>Add A New Title for this Figure: <input type="text" name="title[name]" id="new_title" value=""</input></p>
      <br />

      <label>Landmark List</label>
      <% Landmark.all.each do |landmark| %>
        <%if @figure.landmarks.include?(landmark)%>
          <p><input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>" checked><%=landmark.name%></input></p>
        <%else %>
          <p><input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>"><%=landmark.name%></input></p>
        <%end%>
      <%end%>

      <br />

      <p>Add A New Landmark for this Figure: <input type="text" id="new_landmark" name="landmark[name]"</input></p>
      <label>Year Completed</label>
      <input type="text" name="landmark[year_completed]"/>

      <br />
      <br />
      <input type="submit" value="Edit Figure" />
    </form>

  </body>
</html>
